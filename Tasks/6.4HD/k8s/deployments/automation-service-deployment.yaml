apiVersion: apps/v1
kind: Deployment
metadata:
  name: automation-service
  labels:
    app: automation-service
    tier: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: automation-service
  template:
    metadata:
      labels:
        app: automation-service
        tier: backend
    spec:
      containers:
      - name: automation-service
        image: iot-automation-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3003
          name: http
        env:
        - name: PORT
          value: "3003"
        - name: MONGODB_URI
          valueFrom:
            secretKeyRef:
              name: mongodb-secret
              key: connectionString
        - name: IOT_HOST
          valueFrom:
            configMapKeyRef:
              name: iot-config
              key: iotHost
        - name: SENSOR_DATA_SERVICE
          value: "http://sensor-data-service:3001"
        - name: LIGHT_CONTROL_SERVICE
          value: "http://light-control-service:3002"
        - name: LIGHT_THRESHOLD
          value: "200"
        - name: CERT_PATH
          value: "/certs/light_sensor.cert.pem"
        - name: KEY_PATH
          value: "/certs/light_sensor.private.key"
        - name: CA_PATH
          value: "/certs/root-CA.crt"
        volumeMounts:
        - name: aws-iot-certs
          mountPath: /certs
          readOnly: true
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 10
          periodSeconds: 5
      volumes:
      - name: aws-iot-certs
        secret:
          secretName: aws-iot-certs

