apiVersion: v1
kind: Service
metadata:
  name: automation-service
  labels:
    app: automation-service
spec:
  type: ClusterIP
  ports:
  - port: 3003
    targetPort: 3003
    protocol: TCP
    name: http
  selector:
    app: automation-service

