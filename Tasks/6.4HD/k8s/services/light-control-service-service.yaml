apiVersion: v1
kind: Service
metadata:
  name: light-control-service
  labels:
    app: light-control-service
spec:
  type: ClusterIP
  ports:
  - port: 3002
    targetPort: 3002
    protocol: TCP
    name: http
  selector:
    app: light-control-service

