apiVersion: v1
kind: Service
metadata:
  name: sensor-data-service
  labels:
    app: sensor-data-service
spec:
  type: ClusterIP
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: sensor-data-service

